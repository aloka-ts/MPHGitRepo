package com.agnity.ph.common.enums;

/**
 * Created by ankitsinghal on 27/03/16.
 */
public enum PersistanceType {

    //Attributes values that needs to be stored in persistable map
    PERSISTABLE,

    //Attributes values that needs to be stored in non-persistable map
    NON_PERSISTABLE
}
